<configuration debug="false">
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <target>System.out</target>
        <immediateFlush>true</immediateFlush>

        <encoder>
            <pattern>[%date{yyyy-MM-dd HH:mm:ss}] [%-5level] [%logger] %msg%n</pattern>
<!--   	        <pattern>[%date{yyyy-MM-dd HH:mm:ss}] [%-5level] [%logger] %msg%n</pattern>-->
   
        </encoder>
    </appender>

    <property name="LOG_DIR" value="${java.io.tmpdir}"/>

    <appender name="FILE"
          class="ch.qos.logback.core.rolling.RollingFileAppender">

        <file>${LOG_DIR}/cryptator.log</file>

        <encoder>
            <pattern>[%date{yyyy-MM-dd HH:mm:ss}] [%-5level] %msg%n</pattern>
        </encoder>

        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">

            <fileNamePattern>
                ${LOG_DIR}/cryptator.%d{yyyy-MM-dd}.%i.log
            </fileNamePattern>

            <maxFileSize>10MB</maxFileSize>
            <maxHistory>7</maxHistory>
            <cleanHistoryOnStart>true</cleanHistoryOnStart>

        </rollingPolicy>
    </appender>

    <root level="WARN">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </root>

    <logger name="cryptator" level="TRACE"/>
</configuration>
