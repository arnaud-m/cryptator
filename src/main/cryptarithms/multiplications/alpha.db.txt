beta * eta * nu = alpha * zeta
beta * eta * xi = alpha * zeta
beta * phi = alpha * nu
beta * pi * phi = alpha * psi
beta * pi * psi = alpha * phi
beta * pi * psi = alpha * xi
beta * xi * phi = alpha * eta
beta * xi * pi = alpha * eta
chi * omega = alpha * phi
chi * omega = alpha * pi
chi * omega = eta * pi * phi
chi * omega = rho * sigma
chi * omega = sigma * phi
chi * psi = alpha
chi * psi = alpha * nu
chi * psi = alpha * xi
chi * psi = sigma
chi * psi = theta * pi
chi * psi = theta * xi
delta * chi = alpha * pi
delta * chi = alpha * psi
delta * iota = beta * pi * tau
delta * iota = beta * xi * tau
delta * nu = beta * tau
delta * pi = beta * iota
delta * psi = alpha * chi
delta * psi = alpha * phi
delta * rho = beta * iota
delta * tau = alpha * nu
delta * xi = beta * iota
epsilon = delta * psi
eta * iota = alpha * nu
eta * iota = gamma * pi
eta * iota = gamma * xi
eta * iota * nu = beta * pi * tau
eta * iota * nu = beta * xi * tau
eta * iota * nu = zeta * pi * tau
eta * iota * nu = zeta * xi * tau
eta * iota * pi = beta * xi * rho
eta * iota * pi = zeta * xi * rho
eta * iota * xi = alpha * chi
eta * iota * xi = alpha * psi
eta * iota * xi = beta * pi * rho
eta * iota * xi = zeta * pi * rho
eta * kappa = beta * omega
eta * kappa = beta * phi
eta * kappa = beta * pi * tau
eta * kappa = beta * psi
eta * kappa = beta * sigma
eta * kappa = gamma * phi
eta * kappa = gamma * psi
eta * kappa = zeta * omega
eta * kappa = zeta * phi
eta * kappa = zeta * pi * tau
eta * kappa = zeta * psi
eta * kappa = zeta * sigma
eta * lambda = beta * iota
eta * nu * chi = beta * pi * phi
eta * nu * chi = zeta * pi * phi
eta * nu * pi = alpha * xi
eta * nu * rho = beta * iota
eta * nu * rho = zeta * iota
eta * nu * tau = alpha * pi
eta * nu * tau = beta * pi * chi
eta * nu * tau = beta * xi * chi
eta * nu * tau = beta * xi * phi
eta * nu * tau = beta * xi * psi
eta * nu * tau = delta * pi
eta * nu * tau = delta * xi
eta * nu * tau = epsilon
eta * nu * tau = zeta * pi * chi
eta * nu * tau = zeta * xi * chi
eta * nu * tau = zeta * xi * phi
eta * nu * tau = zeta * xi * psi
eta * nu * xi = beta * pi * phi
eta * nu * xi = beta * pi * psi
eta * nu * xi = zeta * pi * phi
eta * nu * xi = zeta * pi * psi
eta * omega = beta * gamma
eta * omega = beta * rho
eta * omega = gamma * zeta
eta * omega = zeta * rho
eta * phi = alpha
eta * phi * chi = beta * kappa
eta * phi * chi = beta * nu * pi
eta * phi * chi = beta * pi * rho
eta * phi * chi = gamma * psi
eta * phi * chi = zeta * kappa
eta * phi * chi = zeta * nu * pi
eta * phi * chi = zeta * pi * rho
eta * phi = gamma * pi
eta * pi * chi = alpha * nu
eta * pi * chi = alpha * phi
eta * pi * chi = alpha * psi
eta * pi * chi = beta * tau
eta * pi * chi = delta * phi
eta * pi * chi = delta * tau
eta * pi * chi = gamma * xi
eta * pi * chi = zeta * tau
eta * pi * phi = alpha * chi
eta * pi * phi = beta * nu
eta * pi * phi = beta * psi
eta * pi * phi = beta * tau
eta * pi * phi = beta * xi * tau
eta * pi * phi = delta * chi
eta * pi * phi = delta * nu
eta * pi * phi = delta * tau
eta * pi * phi = gamma * psi
eta * pi * phi = gamma * rho
eta * pi * phi = gamma * xi
eta * pi * phi = zeta * nu
eta * pi * phi = zeta * psi
eta * pi * phi = zeta * tau
eta * pi * phi = zeta * xi * tau
eta * pi * psi = alpha * nu
eta * pi * psi = beta * nu
eta * pi * psi = beta * phi
eta * pi * psi = beta * rho
eta * pi * psi = beta * tau
eta * pi * psi = beta * xi * tau
eta * pi * psi = delta * nu
eta * pi * psi = delta * tau
eta * pi * psi = gamma * phi
eta * pi * psi = gamma * xi
eta * pi * psi = zeta * nu
eta * pi * psi = zeta * phi
eta * pi * psi = zeta * rho
eta * pi * psi = zeta * tau
eta * pi * psi = zeta * xi * tau
eta * pi * rho = alpha * xi
eta * pi * tau = alpha * chi
eta * pi * tau = alpha * nu
eta * pi * tau = alpha * xi
eta * pi * tau = beta * iota
eta * pi * tau = beta * nu * phi
eta * pi * tau = beta * nu * psi
eta * pi * tau = delta * iota
eta * pi * tau = zeta * iota
eta * pi * tau = zeta * nu * phi
eta * pi * tau = zeta * nu * psi
eta * psi = beta * rho
eta * psi = gamma * pi
eta * psi = zeta * rho
eta * rho = beta * psi
eta * rho * chi = alpha * phi
eta * rho * chi = beta * iota
eta * rho * chi = zeta * iota
eta * rho * phi = alpha * psi
eta * rho * phi = beta * iota
eta * rho * phi = zeta * iota
eta * rho * psi = alpha * phi
eta * rho * tau = beta * pi * phi
eta * rho * tau = zeta * pi * phi
eta * rho = zeta * psi
eta * sigma = beta * psi
eta * sigma = delta * psi
eta * sigma = zeta * psi
eta * tau = beta * chi
eta * tau = beta * phi
eta * tau = beta * psi
eta * tau = beta * rho
eta * tau * chi = alpha * nu * pi
eta * tau * chi = beta * nu * phi
eta * tau * chi = delta * phi
eta * tau * chi = zeta * nu * phi
eta * tau = delta * pi
eta * tau = delta * xi
eta * tau * phi = alpha * xi * pi
eta * tau * phi = beta * nu * chi
eta * tau * phi = beta * pi * psi
eta * tau * phi = beta * xi * pi
eta * tau * phi = beta * xi * psi
eta * tau * phi = delta * chi
eta * tau * phi = zeta * nu * chi
eta * tau * phi = zeta * pi * psi
eta * tau * phi = zeta * xi * pi
eta * tau * phi = zeta * xi * psi
eta * tau * psi = alpha * nu * pi
eta * tau * psi = beta * pi * phi
eta * tau * psi = beta * theta
eta * tau * psi = beta * xi * phi
eta * tau * psi = beta * xi * pi
eta * tau * psi = zeta * pi * phi
eta * tau * psi = zeta * theta
eta * tau * psi = zeta * xi * phi
eta * tau * psi = zeta * xi * pi
eta * tau = zeta * chi
eta * tau = zeta * phi
eta * tau = zeta * psi
eta * tau = zeta * rho
eta * theta = gamma * iota
eta * xi * chi = alpha * phi
eta * xi * chi = alpha * pi
eta * xi * chi = alpha * psi
eta * xi * chi = beta * pi * phi
eta * xi * chi = beta * tau
eta * xi * chi = delta * tau
eta * xi * chi = gamma * phi
eta * xi * chi = gamma * pi
eta * xi * chi = zeta * pi * phi
eta * xi * chi = zeta * tau
eta * xi * phi = alpha * nu
eta * xi * phi = alpha * pi
eta * xi * phi = alpha * psi
eta * xi * phi = beta * tau
eta * xi * phi = delta * tau
eta * xi * phi = gamma * chi
eta * xi * phi = gamma * pi
eta * xi * phi = zeta * tau
eta * xi * pi = alpha * chi
eta * xi * psi = alpha * phi
eta * xi * psi = alpha * pi
eta * xi * psi = beta * tau
eta * xi * psi = delta * tau
eta * xi * psi = gamma * pi
eta * xi * psi = zeta * tau
eta * xi * rho = alpha * pi
eta * xi * rho = beta * pi * phi
eta * xi * rho = zeta * pi * phi
eta * xi * tau = alpha * chi
eta * xi * tau = alpha * nu
eta * xi * tau = beta * iota
eta * xi * tau = beta * pi * phi
eta * xi * tau = beta * pi * psi
eta * xi * tau = delta * iota
eta * xi * tau = zeta * iota
eta * xi * tau = zeta * pi * phi
eta * xi * tau = zeta * pi * psi
gamma * chi = beta * pi * phi
gamma * eta = alpha * psi
gamma * phi = alpha * chi
gamma * pi = alpha * chi
gamma * psi = alpha * phi
gamma * psi = alpha * xi
gamma * rho = alpha * psi
gamma * zeta = beta * theta
iota * chi = alpha * phi
iota * chi = beta * eta
iota * chi = beta * nu
iota * chi = beta * rho
iota * chi = delta * eta
iota * chi = delta * rho
iota * chi = eta * nu * phi
iota * chi = eta * nu * tau
iota * chi = eta * pi * phi
iota * chi = theta * nu
iota * chi = theta * phi
iota * chi = zeta * eta
iota * chi = zeta * nu
iota * chi = zeta * rho
iota * kappa = beta * pi * tau
iota * kappa = eta * omega
iota * kappa = eta * sigma
iota * kappa = eta * theta
iota * kappa = gamma * mu * pi
iota * kappa = theta * rho
iota * kappa = zeta * pi * tau
iota * mu * phi = gamma * tau
iota * mu * pi = gamma * nu
iota * mu * pi = gamma * xi
iota * mu * psi = gamma * tau
iota * nu * chi = eta * pi * tau
iota * nu * chi = eta * rho * tau
iota * nu * chi = eta * xi * tau
iota * nu = gamma * pi
iota * nu = gamma * xi
iota * nu * phi = beta * pi * tau
iota * nu * phi = eta * kappa
iota * nu * phi = eta * pi * tau
iota * nu * phi = eta * rho * tau
iota * nu * phi = eta * xi * tau
iota * nu * phi = theta * xi * pi
iota * nu * phi = zeta * pi * tau
iota * nu * pi = alpha * rho
iota * nu * pi = delta * eta
iota * nu * pi = delta * tau
iota * nu * pi = eta * kappa
iota * nu * pi = eta * tau * chi
iota * nu * pi = eta * theta
iota * nu * pi = gamma * mu
iota * nu * pi = gamma * tau
iota * nu * psi = alpha * tau
iota * nu * psi = beta * pi * tau
iota * nu * psi = eta * kappa
iota * nu * psi = eta * pi * tau
iota * nu * psi = eta * xi * tau
iota * nu * psi = zeta * pi * tau
iota * nu * rho = eta * theta
iota * nu * tau = alpha * phi
iota * nu * tau = alpha * psi
iota * nu * tau = eta * pi * rho
iota * nu * tau = eta * rho * chi
iota * nu * tau = eta * rho * phi
iota * nu * tau = eta * xi * rho
iota * nu * xi = alpha * phi
iota * nu * xi = delta * eta
iota * nu * xi = delta * tau
iota * nu * xi = eta * tau * chi
iota * nu * xi = eta * tau * phi
iota * nu * xi = eta * tau * psi
iota * nu * xi = eta * theta
iota * omega = beta * eta * pi
iota * omega = beta * eta * xi
iota * omega = beta * kappa
iota * omega = beta * pi * phi
iota * omega = beta * pi * psi
iota * omega = beta * theta
iota * omega = eta * pi * phi
iota * omega = eta * pi * psi
iota * omega = eta * theta
iota * omega = gamma * xi * pi
iota * omega = zeta * eta * pi
iota * omega = zeta * eta * xi
iota * omega = zeta * kappa
iota * omega = zeta * pi * phi
iota * omega = zeta * pi * psi
iota * omega = zeta * theta
iota * phi = alpha * nu
iota * phi = beta * eta
iota * phi = beta * nu
iota * phi = beta * rho
iota * phi = delta * eta
iota * phi = delta * rho
iota * phi = eta * nu * chi
iota * phi = eta * nu * psi
iota * phi = eta * nu * tau
iota * phi = eta * pi * chi
iota * phi = theta * chi
iota * phi = theta * nu
iota * phi = zeta * eta
iota * phi = zeta * nu
iota * phi = zeta * rho
iota * pi = alpha * nu
iota * pi = alpha * xi
iota * pi = beta * eta
iota * pi = beta * tau
iota * pi * chi = beta * xi * phi
iota * pi * chi = delta * phi
iota * pi * chi = eta * nu * phi
iota * pi * chi = eta * nu * tau
iota * pi * chi = eta * rho * tau
iota * pi * chi = eta * xi * rho
iota * pi * chi = eta * xi * tau
iota * pi * chi = gamma * phi
iota * pi * chi = theta * rho
iota * pi * chi = zeta * xi * phi
iota * pi = eta * tau
iota * pi = gamma * nu
iota * pi = gamma * xi
iota * pi * phi = alpha * chi
iota * pi * phi = beta * nu * tau
iota * pi * phi = beta * rho
iota * pi * phi = beta * xi * chi
iota * pi * phi = beta * xi * tau
iota * pi * phi = delta * eta
iota * pi * phi = eta * nu * chi
iota * pi * phi = eta * omega
iota * pi * phi = eta * rho * tau
iota * pi * phi = eta * xi * tau
iota * pi * phi = theta * psi
iota * pi * phi = zeta * nu * tau
iota * pi * phi = zeta * rho
iota * pi * phi = zeta * xi * chi
iota * pi * phi = zeta * xi * tau
iota * pi * psi = beta * nu * tau
iota * pi * psi = beta * xi * tau
iota * pi * psi = delta * eta
iota * pi * psi = eta * omega
iota * pi * psi = eta * rho * chi
iota * pi * psi = eta * xi * rho
iota * pi * psi = eta * xi * tau
iota * pi * psi = zeta * nu * tau
iota * pi * psi = zeta * xi * tau
iota * pi * rho = delta * eta
iota * pi * rho = delta * phi
iota * pi * rho = eta * chi * psi
iota * pi * rho = eta * nu * phi
iota * pi * rho = eta * tau * psi
iota * pi * rho = eta * xi * psi
iota * pi * rho = gamma * phi
iota * pi * rho = theta * chi
iota * pi * rho = theta * phi
iota * pi * tau = alpha * chi
iota * pi * tau = alpha * nu * xi
iota * pi * tau = alpha * psi
iota * pi * tau = beta * theta
iota * pi * tau = theta * psi
iota * pi * tau = zeta * theta
iota * pi = zeta * eta
iota * pi = zeta * tau
iota * psi = alpha * chi
iota * psi = alpha * nu
iota * psi = beta * eta
iota * psi = beta * nu
iota * psi = delta * eta
iota * psi = eta * nu * phi
iota * psi = eta * nu * tau
iota * psi = eta * pi * rho
iota * psi = gamma * rho
iota * psi = theta * tau
iota * psi = zeta * eta
iota * psi = zeta * nu
iota * rho = alpha * nu
iota * rho = alpha * xi
iota * rho = beta * eta
iota * rho = beta * pi
iota * rho = beta * tau
iota * rho = beta * xi
iota * rho = delta * pi
iota * rho = delta * xi
iota * rho = eta * chi
iota * rho = eta * nu * chi
iota * rho = eta * nu * phi
iota * rho = eta * nu * tau
iota * rho = eta * phi
iota * rho = eta * pi * chi
iota * rho = eta * pi * tau
iota * rho = eta * psi
iota * rho = eta * tau
iota * rho = eta * xi * chi
iota * rho = eta * xi * phi
iota * rho = eta * xi * tau
iota * rho = theta * pi
iota * rho = theta * xi
iota * rho = zeta * eta
iota * rho = zeta * pi
iota * rho = zeta * tau
iota * rho = zeta * xi
iota * sigma = beta * eta * pi
iota * sigma = beta * eta * xi
iota * sigma = delta * eta
iota * sigma = zeta * eta * pi
iota * sigma = zeta * eta * xi
iota * tau = alpha * rho
iota * tau = delta * chi
iota * tau = delta * eta
iota * tau = delta * phi
iota * tau = delta * psi
iota * tau = eta * chi
iota * tau = eta * nu * rho
iota * tau = eta * phi
iota * tau = eta * psi
iota * xi = beta * eta
iota * xi = beta * tau
iota * xi * chi = beta * pi * phi
iota * xi * chi = eta * nu * tau
iota * xi * chi = eta * phi * psi
iota * xi * chi = eta * pi * phi
iota * xi * chi = eta * pi * rho
iota * xi * chi = eta * pi * tau
iota * xi * chi = eta * rho * tau
iota * xi * chi = gamma * phi
iota * xi * chi = theta * psi
iota * xi * chi = theta * rho
iota * xi * chi = zeta * pi * phi
iota * xi = eta * tau
iota * xi = gamma * pi
iota * xi * phi = alpha * nu * pi
iota * xi * phi = eta * nu * tau
iota * xi * phi = eta * pi * chi
iota * xi * phi = eta * pi * rho
iota * xi * phi = eta * pi * tau
iota * xi * phi = eta * rho * tau
iota * xi * phi = eta * tau * psi
iota * xi * phi = gamma * chi
iota * xi * phi = theta * rho
iota * xi * pi = alpha * phi
iota * xi * pi = eta * kappa
iota * xi * pi = theta * psi
iota * xi * psi = alpha * rho
iota * xi * psi = eta * nu * tau
iota * xi * psi = eta * pi * tau
iota * xi * psi = eta * rho * phi
iota * xi * psi = eta * tau * phi
iota * xi * psi = theta * chi
iota * xi * rho = delta * eta
iota * xi * rho = eta * pi * psi
iota * xi * rho = theta * chi
iota * xi * rho = theta * phi
iota * xi * tau = alpha * phi
iota * xi * tau = beta * theta
iota * xi * tau = zeta * theta
iota * xi = zeta * eta
iota * xi = zeta * tau
kappa * chi = beta * eta * pi
kappa * chi = beta * pi * psi
kappa * chi = beta * xi * pi
kappa * chi = eta * pi * tau
kappa * chi = eta * tau * phi
kappa * chi = eta * xi * phi
kappa * chi = eta * xi * tau
kappa * chi = gamma * xi
kappa * chi = iota * pi * phi
kappa * chi = theta * phi
kappa * chi = theta * pi
kappa * chi = theta * rho
kappa * chi = zeta * eta * pi
kappa * chi = zeta * pi * psi
kappa * chi = zeta * xi * pi
kappa = eta * pi
kappa = iota * pi
kappa * mu = gamma * tau
kappa * mu = iota * nu * pi
kappa * mu = iota * nu * xi
kappa * mu = iota * pi
kappa * mu = iota * xi
kappa * mu * pi = iota * xi * tau
kappa * nu = eta * iota
kappa * nu = eta * pi * tau
kappa * nu = iota * mu * pi
kappa * nu = iota * mu * xi
kappa * nu = iota * pi
kappa * nu = iota * rho
kappa * nu = iota * xi
kappa * nu * pi = eta * tau * phi
kappa * nu * pi = eta * tau * psi
kappa * nu * pi = eta * theta
kappa * nu * pi = eta * xi * phi
kappa * nu * pi = eta * xi * psi
kappa * nu * pi = gamma * phi
kappa * nu * pi = gamma * psi
kappa * nu * pi = iota * xi * tau
kappa * nu * pi = theta * chi
kappa * nu * pi = theta * iota
kappa * nu * pi = theta * psi
kappa * nu = theta * chi
kappa * nu = theta * rho
kappa * nu = theta * xi
kappa * nu * xi = eta * pi * tau
kappa * nu * xi = eta * tau * phi
kappa * nu * xi = eta * tau * psi
kappa * phi = alpha * chi
kappa * phi = beta * iota
kappa * phi = beta * rho
kappa * phi = beta * xi * chi
kappa * phi = beta * xi * tau
kappa * phi = delta * tau
kappa * phi = delta * xi
kappa * phi = eta * nu * tau
kappa * phi = eta * pi * psi
kappa * phi = eta * xi * psi
kappa * phi = gamma * nu * xi
kappa * phi = gamma * xi
kappa * phi = iota * chi
kappa * phi = iota * mu * xi
kappa * phi = iota * nu * xi
kappa * phi = iota * xi * pi
kappa * phi = zeta * iota
kappa * phi = zeta * rho
kappa * phi = zeta * xi * chi
kappa * phi = zeta * xi * tau
kappa * pi = beta * nu
kappa * pi = beta * tau
kappa * pi = epsilon
kappa * pi = eta * nu * tau
kappa * pi = eta * omega
kappa * pi = eta * rho
kappa * pi = eta * tau * chi
kappa * pi = gamma * eta
kappa * pi = iota * phi
kappa * pi = iota * psi
kappa * pi = theta * xi
kappa * pi = zeta * nu
kappa * pi = zeta * tau
kappa * psi = alpha * eta
kappa * psi = beta * iota
kappa * psi = beta * pi * chi
kappa * psi = beta * xi * tau
kappa * psi = delta * tau
kappa * psi = delta * xi
kappa * psi = eta * nu * tau
kappa * psi = eta * pi * phi
kappa * psi = eta * sigma
kappa * psi = eta * xi * chi
kappa * psi = eta * xi * phi
kappa * psi = gamma * nu * xi
kappa * psi = gamma * rho
kappa * psi = gamma * xi
kappa * psi = iota * mu * xi
kappa * psi = iota * nu * xi
kappa * psi = iota * pi * chi
kappa * psi = iota * xi * chi
kappa * psi = iota * xi * pi
kappa * psi = theta * chi
kappa * psi = theta * nu
kappa * psi = zeta * iota
kappa * psi = zeta * pi * chi
kappa * psi = zeta * xi * tau
kappa * rho = beta * pi * phi
kappa * rho = eta * pi * phi
kappa * rho = eta * xi * phi
kappa * rho = gamma * phi
kappa * rho = gamma * psi
kappa * rho = iota * nu * tau
kappa * rho = iota * xi * phi
kappa * rho = theta * chi
kappa * rho = zeta * pi * phi
kappa * tau = alpha * chi
kappa * tau = alpha * rho
kappa * tau = beta * nu * phi
kappa * tau = beta * nu * pi
kappa * tau = beta * nu * psi
kappa * tau = beta * nu * xi
kappa * tau = delta * xi
kappa * tau = eta * iota * nu
kappa * tau = eta * iota * xi
kappa * tau = eta * nu * chi
kappa * tau = eta * nu * phi
kappa * tau = eta * nu * psi
kappa * tau = eta * nu * rho
kappa * tau = eta * xi * phi
kappa * tau = eta * xi * pi
kappa * tau = eta * xi * psi
kappa * tau = iota * nu * xi
kappa * tau = iota * xi * chi
kappa * tau = theta * phi
kappa * tau = theta * psi
kappa * tau = zeta * nu * phi
kappa * tau = zeta * nu * pi
kappa * tau = zeta * nu * psi
kappa * tau = zeta * nu * xi
kappa * xi = beta * nu * pi
kappa * xi = beta * tau
kappa * xi = delta * pi
kappa * xi = eta * iota
kappa * xi = eta * nu * pi
kappa * xi = eta * pi * phi
kappa * xi = eta * pi * psi
kappa * xi = gamma * chi
kappa * xi = iota * mu
kappa * xi = iota * mu * pi
kappa * xi = iota * nu
kappa * xi = iota * nu * pi
kappa * xi = iota * phi
kappa * xi = iota * psi
kappa * xi = iota * tau
kappa * xi * pi = beta * eta * nu
kappa * xi * pi = beta * iota
kappa * xi * pi = eta * omega
kappa * xi * pi = eta * phi * chi
kappa * xi * pi = eta * tau * phi
kappa * xi * pi = eta * tau * psi
kappa * xi * pi = gamma * chi
kappa * xi * pi = theta * chi
kappa * xi * pi = zeta * eta * nu
kappa * xi * pi = zeta * iota
kappa * xi = theta * nu
kappa * xi = theta * rho
kappa * xi = theta * tau
kappa * xi = zeta * nu * pi
kappa * xi = zeta * tau
lambda = beta * eta
lambda * chi = kappa * phi
lambda = delta * pi
lambda = delta * xi
lambda = eta * chi
lambda = eta * phi
lambda = eta * pi * phi
lambda = eta * pi * psi
lambda = eta * psi
lambda = eta * rho
lambda * phi = beta * eta * pi
lambda * pi = alpha * psi
lambda * pi = alpha * xi
lambda * pi = beta * zeta
lambda * pi = delta * phi
lambda * pi = delta * psi
lambda * pi = kappa * phi
lambda * pi = kappa * psi
lambda * psi = beta * eta * pi
lambda * xi = alpha * pi
lambda * xi = beta * zeta
mu * nu * phi = gamma * pi
mu * nu * pi = gamma * xi
mu * nu * pi * phi = gamma * psi
mu * nu * pi * psi = gamma * phi
mu * nu * psi = gamma * pi
mu * phi = gamma
mu * phi = iota * xi
mu * pi * phi = gamma * xi
mu * pi * phi = iota * nu * xi
mu * pi * phi = kappa * nu
mu * pi * psi = gamma * xi
mu * pi * psi = iota * nu * xi
mu * pi * psi = kappa * nu
mu * pi * sigma = iota * nu * psi
mu * psi = gamma
mu * psi = iota * xi
mu * sigma = iota * phi
mu * sigma = iota * pi
mu * sigma = iota * xi
mu * sigma = kappa * nu
mu * sigma = kappa * pi
mu * sigma = kappa * tau
mu * xi * phi = iota * nu * pi
mu * xi * phi = iota * psi
mu * xi * pi = kappa * nu
mu * xi * pi * phi = iota * tau
mu * xi * pi * psi = iota * tau
mu * xi * psi = iota * nu * pi
mu * xi * psi = iota * phi
nu * chi = iota * pi
nu * chi = iota * xi
nu * chi * psi = iota * pi * tau
nu * chi * psi = kappa * pi
nu * chi * psi = theta * tau
nu * phi = alpha
nu * phi * chi = alpha * xi * pi
nu * phi * chi = epsilon
nu * phi * chi = iota * psi
nu * phi * chi = iota * xi * pi
nu * phi * chi = kappa * rho
nu * phi * chi = kappa * xi * pi
nu * phi * chi = theta * pi
nu * phi = eta * psi
nu * phi = iota * xi
nu * phi * psi = eta * chi
nu * pi * chi = alpha * tau
nu * pi * chi = eta * kappa
nu * pi * phi = beta * eta
nu * pi * phi = delta
nu * pi * phi = delta * tau
nu * pi * phi = gamma
nu * pi * phi = iota * mu * xi
nu * pi * phi = kappa * mu
nu * pi * phi = mu * tau * psi
nu * pi * phi = theta * xi
nu * pi * phi = zeta * eta
nu * pi * psi = alpha * xi
nu * pi * psi = beta * eta
nu * pi * psi = delta
nu * pi * psi = delta * tau
nu * pi * psi = gamma
nu * pi * psi = iota * chi
nu * pi * psi = iota * mu * xi
nu * pi * psi = iota * rho
nu * pi * psi = kappa * mu
nu * pi * psi = mu * tau * phi
nu * pi * psi = theta
nu * pi * psi = theta * xi
nu * pi * psi = zeta * eta
nu * pi * sigma = gamma * mu * xi
nu * pi * sigma = iota * mu * psi
nu * pi * tau = delta * xi
nu * pi * tau = epsilon
nu * pi * tau = eta * kappa
nu * pi * tau = iota * chi
nu * pi * tau = iota * phi
nu * pi * tau = iota * psi
nu * pi * tau = iota * rho
nu * pi * tau = iota * xi
nu * psi = eta * phi
nu * psi = iota * xi
nu * rho * chi = alpha * phi
nu * rho * chi = iota * phi
nu * rho * chi = iota * tau
nu * rho * chi = kappa * pi
nu * rho * phi = iota * chi
nu * rho * phi = iota * tau
nu * rho * phi = kappa * chi
nu * rho * phi = kappa * pi
nu * rho * phi = kappa * tau
nu * rho * phi = kappa * xi
nu * rho * psi = alpha * phi
nu * rho * psi = iota * phi
nu * rho * tau = iota * psi
nu * rho * tau = kappa * pi
nu * sigma = gamma * phi
nu * sigma = gamma * tau
nu * sigma = iota * mu * pi
nu * sigma = iota * mu * xi
nu * sigma = iota * pi
nu * sigma = iota * xi
nu * sigma = kappa * pi
nu * sigma = kappa * tau
nu * sigma = mu * tau * psi
nu * sigma = mu * xi * phi
nu * tau = alpha
nu * tau * chi = alpha * psi
nu * tau * chi = eta * xi * phi
nu * tau * chi = iota * pi * rho
nu * tau * chi = iota * xi * phi
nu * tau * chi = iota * xi * rho
nu * tau * chi = kappa * psi
nu * tau * chi = kappa * xi
nu * tau = kappa
nu * tau * phi = alpha * pi
nu * tau * phi = beta * psi
nu * tau * phi = delta * pi
nu * tau * phi = eta * pi * rho
nu * tau * phi = eta * xi * chi
nu * tau * phi = iota * psi
nu * tau * phi = iota * xi * chi
nu * tau * phi = iota * xi * pi
nu * tau * phi = iota * xi * rho
nu * tau * phi = kappa * pi
nu * tau * phi = kappa * psi
nu * tau * phi = theta * psi
nu * tau * phi = zeta * psi
nu * tau * psi = alpha * xi
nu * tau * psi = beta * phi
nu * tau * psi = delta * pi
nu * tau * psi = eta * pi * chi
nu * tau * psi = iota * phi
nu * tau * psi = iota * xi * pi
nu * tau * psi = kappa * phi
nu * tau * psi = kappa * pi
nu * tau * psi = theta * chi
nu * tau * psi = theta * xi
nu * tau * psi = zeta * phi
nu * xi * chi = alpha * pi
nu * xi * chi = alpha * psi
nu * xi * chi = kappa * pi
nu * xi * phi = alpha * pi
nu * xi * phi = iota * mu * pi
nu * xi * phi = iota * psi
nu * xi * pi * chi = alpha * phi
nu * xi * pi * chi = eta * tau * phi
nu * xi * pi * chi = theta * phi
nu * xi * pi = kappa * mu
nu * xi * pi * phi = beta * eta
nu * xi * pi * phi = eta * iota
nu * xi * pi * phi = eta * tau * psi
nu * xi * pi * phi = eta * theta
nu * xi * pi * phi = iota * chi
nu * xi * pi * phi = iota * tau
nu * xi * pi * phi = theta * iota
nu * xi * pi * phi = theta * psi
nu * xi * pi * phi = zeta * eta
nu * xi * pi * psi = beta * eta
nu * xi * pi * psi = eta * iota
nu * xi * pi * psi = eta * tau * phi
nu * xi * pi * psi = iota * tau
nu * xi * pi * psi = theta * phi
nu * xi * pi * psi = zeta * eta
nu * xi * pi * tau = alpha * psi
nu * xi * pi * tau = theta * phi
nu * xi * psi = alpha * pi
nu * xi * psi = iota * mu * pi
nu * xi * psi = iota * phi
nu * xi * psi = mu * sigma
nu * xi * rho = alpha * pi
nu * xi * sigma = kappa * psi
nu * xi * sigma = mu * tau * psi
nu * xi * tau = alpha * chi
nu * xi * tau = delta * pi
nu * xi * tau = eta * kappa
nu * xi * tau = iota * chi
nu * xi * tau = iota * phi
nu * xi * tau = iota * pi
nu * xi * tau = iota * psi
nu * xi * tau = iota * rho
omicron = eta * iota
phi * chi = pi * sigma
phi * chi * psi = alpha * xi * pi
phi * chi * psi = beta * eta
phi * chi * psi = eta * pi * rho
phi * chi * psi = iota * nu * pi
phi * chi * psi = iota * pi * rho
phi * chi * psi = pi * rho * tau
phi * chi * psi = theta * rho
phi * chi * psi = theta * xi * pi
phi * chi * psi = xi * pi * sigma
phi * chi * psi = zeta * eta
phi * chi = rho * psi
phi * omega = alpha * psi
phi * omega = beta * eta * pi
phi * omega = eta * iota * pi
phi * omega = gamma * psi
phi * omega = iota * pi * rho
phi * omega = iota * xi * pi
phi * omega = pi * rho * psi
phi * omega = rho * sigma
phi * omega = sigma * chi
phi * omega = zeta * eta * pi
phi * psi = beta * chi
phi * psi = eta * xi * rho
phi * psi = iota * chi
phi * psi = iota * rho
phi * psi = sigma
phi * psi = theta * nu
phi * psi = xi * rho * tau
phi * psi = xi * sigma
phi * psi = zeta * chi
pi * chi = eta * tau
pi * chi = gamma
pi * chi = iota * xi
pi * chi = kappa
pi * chi * psi = alpha * eta
pi * chi * psi = beta * eta
pi * chi * psi = beta * xi * phi
pi * chi * psi = eta * iota * xi
pi * chi * psi = eta * sigma
pi * chi * psi = eta * tau
pi * chi * psi = iota * nu
pi * chi * psi = iota * nu * tau
pi * chi * psi = iota * rho
pi * chi * psi = iota * xi * tau
pi * chi * psi = theta * nu
pi * chi * psi = theta * xi
pi * chi * psi = xi * sigma
pi * chi * psi = zeta * eta
pi * chi * psi = zeta * xi * phi
pi * omega = delta * eta
pi * omega = eta * xi * phi
pi * omega = eta * xi * psi
pi * omega = gamma * chi
pi * omega = iota * rho
pi * omega = kappa * xi
pi * phi * chi = beta * iota
pi * phi * chi = beta * nu
pi * phi * chi = beta * xi * psi
pi * phi * chi = delta * eta
pi * phi * chi = delta * xi
pi * phi * chi = eta * nu * psi
pi * phi * chi = iota * kappa
pi * phi * chi = iota * nu * xi
pi * phi * chi = kappa * xi
pi * phi * chi = nu * rho * psi
pi * phi * chi = xi * omega
pi * phi * chi = zeta * iota
pi * phi * chi = zeta * nu
pi * phi * chi = zeta * xi * psi
pi * phi = eta * nu * xi
pi * phi = eta * tau
pi * phi = mu * psi
pi * phi = nu * chi
pi * phi = nu * psi
pi * phi = nu * xi * chi
pi * phi * psi = alpha * eta
pi * phi * psi = beta * chi
pi * phi * psi = beta * xi * chi
pi * phi * psi = eta * rho * tau
pi * phi * psi = eta * xi * rho
pi * phi * psi = theta * rho
pi * phi * psi = xi * rho * tau
pi * phi * psi = xi * sigma
pi * phi * psi = zeta * chi
pi * phi * psi = zeta * xi * chi
pi * psi = eta * nu * xi
pi * psi = eta * tau
pi * psi = mu * phi
pi * psi = nu * phi
pi * psi = nu * xi * rho
pi * rho = alpha
pi * rho * chi = eta * kappa
pi * rho * chi = eta * tau * phi
pi * rho * chi = eta * theta
pi * rho * chi = iota * psi
pi * rho * chi = iota * tau
pi * rho * chi = iota * xi * psi
pi * rho * chi = nu * tau * phi
pi * rho * chi = theta * xi
pi * rho = kappa
pi * rho * phi = beta * eta
pi * rho * phi = beta * xi
pi * rho * phi = eta * chi * psi
pi * rho * phi = eta * iota
pi * rho * phi = eta * omega
pi * rho * phi = eta * tau
pi * rho * phi = eta * theta
pi * rho * phi = eta * xi * tau
pi * rho * phi = gamma * xi
pi * rho * phi = kappa * chi
pi * rho * phi = nu * tau * psi
pi * rho * phi = nu * xi * tau
pi * rho * phi = theta * nu
pi * rho * phi = theta * psi
pi * rho * phi = xi * omega
pi * rho * phi = zeta * eta
pi * rho * phi = zeta * xi
pi * rho * psi = alpha * xi
pi * rho * psi = eta * chi
pi * rho * psi = eta * tau * phi
pi * rho * psi = eta * theta
pi * rho * psi = iota * chi
pi * rho * psi = iota * nu * phi
pi * rho * psi = iota * tau
pi * rho * psi = iota * xi
pi * rho * psi = iota * xi * tau
pi * rho * psi = nu * tau * phi
pi * rho * psi = theta * chi
pi * rho * tau = alpha * xi
pi * rho * tau = eta * iota
pi * rho * tau = iota * nu * psi
pi * rho * tau = iota * nu * xi
pi * rho * tau = kappa * phi
pi * rho * tau = kappa * psi
pi * rho * tau = theta * nu
pi * rho * tau = xi * phi * chi
pi * sigma = beta * eta
pi * sigma = beta * xi
pi * sigma = iota * mu
pi * sigma = iota * rho
pi * sigma = kappa * chi
pi * sigma = kappa * mu
pi * sigma = lambda
pi * sigma = mu * nu * psi
pi * sigma = mu * tau * psi
pi * sigma = nu * tau * psi
pi * sigma = nu * xi * phi
pi * sigma = theta * xi
pi * sigma = xi * tau * phi
pi * sigma = xi * tau * psi
pi * sigma = zeta * eta
pi * sigma = zeta * xi
pi * tau * chi = alpha * beta
pi * tau * chi = alpha * zeta
pi * tau * chi = beta * nu * phi
pi * tau * chi = delta * phi
pi * tau * chi = eta * iota
pi * tau * chi = eta * kappa
pi * tau * chi = eta * phi * psi
pi * tau * chi = iota * nu * xi
pi * tau * chi = iota * psi
pi * tau * chi = iota * rho
pi * tau * chi = kappa * psi
pi * tau * chi = kappa * xi
pi * tau * chi = theta * xi
pi * tau * chi = zeta * nu * phi
pi * tau = iota * nu
pi * tau * phi = alpha * rho
pi * tau * phi = alpha * xi
pi * tau * phi = beta * chi
pi * tau * phi = beta * eta
pi * tau * phi = beta * xi
pi * tau * phi = beta * xi * chi
pi * tau * phi = delta * chi
pi * tau * phi = delta * psi
pi * tau * phi = eta * iota * nu
pi * tau * phi = eta * nu * rho
pi * tau * phi = eta * rho
pi * tau * phi = eta * rho * chi
pi * tau * phi = iota * nu * rho
pi * tau * phi = iota * nu * xi
pi * tau * phi = iota * psi
pi * tau * phi = iota * rho
pi * tau * phi = kappa * nu * xi
pi * tau * phi = theta * chi
pi * tau * phi = theta * psi
pi * tau * phi = theta * rho
pi * tau * phi = zeta * chi
pi * tau * phi = zeta * eta
pi * tau * phi = zeta * xi
pi * tau * phi = zeta * xi * chi
pi * tau * psi = alpha * rho
pi * tau * psi = beta * chi
pi * tau * psi = beta * eta
pi * tau * psi = beta * xi
pi * tau * psi = delta * phi
pi * tau * psi = eta * chi
pi * tau * psi = eta * iota * nu
pi * tau * psi = eta * rho
pi * tau * psi = eta * xi * chi
pi * tau * psi = iota * chi
pi * tau * psi = iota * nu * xi
pi * tau * psi = iota * phi
pi * tau * psi = kappa * nu * xi
pi * tau * psi = nu * rho * phi
pi * tau * psi = theta * nu
pi * tau * psi = theta * xi
pi * tau * psi = zeta * chi
pi * tau * psi = zeta * eta
pi * tau * psi = zeta * xi
pi * upsilon = alpha * nu * xi
pi * upsilon = iota * xi * tau
psi * omega = beta * eta * pi
psi * omega = eta * iota * pi
psi * omega = gamma * phi
psi * omega = gamma * rho
psi * omega = iota * xi * pi
psi * omega = sigma * chi
psi * omega = sigma * phi
psi * omega = xi * sigma
psi * omega = zeta * eta * pi
rho * chi = iota * psi
rho * chi = kappa * pi
rho * chi * psi = iota * pi * phi
rho * chi * psi = nu * xi * pi * phi
rho * chi * psi = theta * phi
rho * chi = theta * pi
rho * chi = theta * xi
rho * chi = xi * pi * phi
rho * omega = delta * eta
rho * omega = gamma * chi
rho * omega = gamma * phi
rho * phi = alpha
rho * phi * chi = eta * iota * pi
rho * phi * chi = iota * nu * pi
rho * phi = iota * pi
rho * phi = kappa * nu
rho * phi = nu * psi
rho * phi * psi = alpha * nu * pi
rho * phi * psi = eta * iota * pi
rho * phi * psi = eta * pi * tau
rho * phi * psi = iota * sigma
rho * phi * psi = iota * xi * chi
rho * phi * psi = iota * xi * pi
rho * phi = theta * xi
rho * phi = xi * pi * chi
rho * psi = alpha * xi
rho * psi = beta * phi
rho * psi = iota * chi
rho * psi = xi * sigma
rho * psi = zeta * phi
rho * sigma = pi * chi * psi
rho * sigma = pi * phi * psi
rho * tau = alpha * pi
rho * tau = alpha * xi
rho * tau = eta * pi * psi
rho * tau = iota * psi
rho * tau = kappa * nu
rho * tau * phi = alpha * chi
rho * tau * phi = alpha * nu * pi
rho * tau = xi * phi * psi
sigma * chi = alpha * phi
sigma * chi = beta * eta
sigma * chi = eta * iota
sigma * chi = eta * phi * psi
sigma * chi = eta * pi * phi
sigma * chi = gamma * psi
sigma * chi = iota * pi * psi
sigma * chi = iota * psi
sigma * chi = kappa * pi
sigma * chi = rho * omega
sigma * chi = theta * phi
sigma * chi = zeta * eta
sigma = mu * tau
sigma = nu * pi * phi
sigma = nu * pi * psi
sigma * phi = alpha * chi
sigma * phi = beta * eta
sigma * phi = eta * iota
sigma * phi = iota * mu * pi
sigma * phi = iota * pi * chi
sigma * phi = iota * pi * rho
sigma * phi = kappa * mu * nu
sigma * phi = kappa * mu * pi
sigma * phi = kappa * tau
sigma * phi = kappa * xi
sigma * phi = lambda * pi
sigma * phi = mu * nu * xi * psi
sigma * phi = nu * tau * psi
sigma * phi = nu * xi * pi * psi
sigma * phi = rho * omega
sigma * phi = theta * chi
sigma * phi = xi * chi * psi
sigma * phi = zeta * eta
sigma = pi * phi
sigma * psi = alpha * chi
sigma * psi = alpha * xi
sigma * psi = beta * iota
sigma * psi = beta * omega
sigma * psi = eta * pi * phi
sigma * psi = eta * theta
sigma * psi = eta * xi * phi
sigma * psi = gamma * nu * xi
sigma * psi = iota * chi
sigma * psi = iota * mu * nu
sigma * psi = iota * mu * tau
sigma * psi = iota * nu * pi
sigma * psi = iota * pi * chi
sigma * psi = lambda * xi
sigma * psi = mu * nu * pi * phi
sigma * psi = nu * xi * pi * tau
sigma * psi = nu * xi * tau
sigma * psi = rho * omega
sigma * psi = theta * phi
sigma * psi = theta * xi
sigma * psi = xi * omega
sigma * psi = zeta * iota
sigma * psi = zeta * omega
tau * chi = alpha * pi
tau * chi = alpha * xi
tau * chi = eta * pi * phi
tau * chi = eta * pi * psi
tau * chi = iota * psi
tau * chi = iota * rho
tau * chi = kappa * nu
tau * chi = pi * rho * psi
tau * chi * psi = alpha * nu * pi
tau * chi * psi = eta * iota * pi
tau * chi * psi = eta * xi * phi
tau * chi * psi = iota * xi * pi
tau * chi * psi = pi * rho * phi
tau * chi = xi * phi * psi
tau * phi = alpha * xi
tau * phi * chi = beta * eta * pi
tau * phi * chi = eta * nu * xi * pi
tau * phi * chi = kappa * nu * xi
tau * phi * chi = kappa * xi * pi
tau * phi * chi = zeta * eta * pi
tau * phi = iota * rho
tau * phi * psi = alpha * xi * pi
tau * phi * psi = eta * nu * chi
tau * phi * psi = eta * pi * chi
tau * phi * psi = eta * pi * rho
tau * phi * psi = iota * pi * rho
tau * phi * psi = kappa * chi
tau * psi = alpha * xi
tau * psi = iota * chi
theta = beta * nu
theta = beta * pi
theta = beta * xi
theta * chi = alpha * nu
theta * chi = beta * nu * tau
theta * chi = beta * pi * phi
theta * chi = beta * xi * phi
theta * chi = delta * tau
theta * chi = eta * nu * phi
theta * chi = eta * nu * psi
theta * chi = eta * nu * tau
theta * chi = eta * pi * psi
theta * chi = eta * rho * tau
theta * chi = eta * tau * psi
theta * chi = gamma * rho
theta * chi = zeta * nu * tau
theta * chi = zeta * pi * phi
theta * chi = zeta * xi * phi
theta = eta * chi
theta = eta * phi
theta = eta * psi
theta = eta * rho
theta * iota = alpha * chi
theta * iota = alpha * xi * pi
theta * iota = beta * eta * pi
theta * iota = beta * eta * xi
theta * iota = delta * eta
theta * iota = eta * sigma
theta * iota = zeta * eta * pi
theta * iota = zeta * eta * xi
theta * nu = alpha * tau
theta * nu = eta * pi * chi
theta * nu = eta * pi * rho
theta * nu = eta * psi
theta * nu = eta * xi * chi
theta * nu = eta * xi * phi
theta * nu = eta * xi * rho
theta * nu * pi = alpha * beta
theta * nu * pi = alpha * zeta
theta * nu * pi = eta * kappa
theta * nu * pi = eta * phi * chi
theta * nu * xi = eta * pi * psi
theta * phi = beta * nu * tau
theta * phi = beta * xi * chi
theta * phi = delta * psi
theta * phi = delta * tau
theta * phi = eta * nu * chi
theta * phi = eta * nu * psi
theta * phi = eta * nu * tau
theta * phi = eta * pi * psi
theta * phi = eta * rho * tau
theta * phi = eta * xi * psi
theta * phi = gamma * rho
theta * phi = zeta * nu * tau
theta * phi = zeta * xi * chi
theta * pi = beta * phi
theta * pi = beta * tau
theta * pi = eta * nu * chi
theta * pi = eta * nu * rho
theta * pi = eta * nu * tau
theta * pi = eta * xi * rho
theta * pi = zeta * phi
theta * pi = zeta * tau
theta * psi = alpha * rho
theta * psi = alpha * xi
theta * psi = beta * pi * chi
theta * psi = beta * pi * phi
theta * psi = beta * pi * tau
theta * psi = beta * xi * chi
theta * psi = delta * chi
theta * psi = delta * tau
theta * psi = eta * iota * nu
theta * psi = eta * iota * xi
theta * psi = eta * nu * tau
theta * psi = eta * nu * xi
theta * psi = eta * nu * xi * pi
theta * psi = eta * pi * phi
theta * psi = eta * pi * rho
theta * psi = eta * rho * phi
theta * psi = eta * xi * phi
theta * psi = zeta * pi * chi
theta * psi = zeta * pi * phi
theta * psi = zeta * pi * tau
theta * psi = zeta * xi * chi
theta * rho = alpha * nu
theta * rho = alpha * xi
theta * rho = beta * nu * tau
theta * rho = beta * pi * phi
theta * rho = delta * tau
theta * rho = eta * nu * tau
theta * rho = eta * pi * chi
theta * rho = eta * pi * psi
theta * rho = eta * tau * chi
theta * rho = eta * tau * phi
theta * rho = eta * xi * chi
theta * rho = eta * xi * phi
theta * rho = eta * xi * psi
theta * rho = gamma * chi
theta * rho = gamma * iota
theta * rho = gamma * phi
theta * rho = gamma * pi
theta * rho = gamma * xi
theta * rho = zeta * nu * tau
theta * rho = zeta * pi * phi
theta * tau = alpha * xi
theta * tau = beta * eta * nu
theta * tau = beta * nu * psi
theta * tau = beta * xi * psi
theta * tau = delta * chi
theta * tau = delta * iota
theta * tau = delta * nu * pi
theta * tau = delta * nu * xi
theta * tau = delta * phi
theta * tau = delta * psi
theta * tau = delta * rho
theta * tau = eta * iota * nu
theta * tau = eta * nu * chi
theta * tau = eta * nu * phi
theta * tau = eta * nu * pi
theta * tau = eta * nu * rho
theta * tau = eta * nu * xi
theta * tau = eta * pi * phi
theta * tau = eta * pi * rho
theta * tau = eta * xi * rho
theta * tau = zeta * eta * nu
theta * tau = zeta * nu * psi
theta * tau = zeta * xi * psi
theta * xi = beta * pi * phi
theta * xi = beta * tau
theta * xi = delta * psi
theta * xi = eta * nu * chi
theta * xi = eta * nu * phi
theta * xi = eta * nu * rho
theta * xi = eta * nu * tau
theta * xi = eta * pi * rho
theta * xi = gamma * psi
theta * xi * pi = beta * nu * phi
theta * xi * pi = eta * rho * phi
theta * xi * pi = gamma * phi
theta * xi * pi = zeta * nu * phi
theta * xi = zeta * pi * phi
theta * xi = zeta * tau
theta = zeta * nu
theta = zeta * pi
theta = zeta * xi
upsilon = eta * iota
upsilon = kappa * pi
upsilon = nu * xi * rho
upsilon = nu * xi * tau
upsilon = pi * tau * phi
upsilon = xi * pi * rho
xi * chi = eta * tau
xi * chi = gamma
xi * chi = iota * pi
xi * chi = kappa
xi * chi * psi = alpha * eta
xi * chi * psi = alpha * tau
xi * chi * psi = beta * phi
xi * chi * psi = gamma * pi
xi * chi * psi = nu * pi * rho
xi * chi * psi = nu * pi * tau
xi * chi * psi = nu * tau * phi
xi * chi * psi = zeta * phi
xi * omega = delta * eta
xi * omega = gamma * chi
xi * omega = gamma * phi
xi * omega = gamma * psi
xi * omega = iota * pi * phi
xi * omega = iota * pi * psi
xi * omega = iota * rho
xi * omega = kappa * phi
xi * omega = kappa * psi
xi * phi = alpha
xi * phi * chi = beta * eta * pi
xi * phi * chi = beta * pi * psi
xi * phi * chi = beta * psi
xi * phi * chi = eta * iota * pi
xi * phi * chi = eta * pi * rho
xi * phi * chi = iota * pi * tau
xi * phi * chi = kappa * nu * pi
xi * phi * chi = kappa * psi
xi * phi * chi = zeta * eta * pi
xi * phi * chi = zeta * pi * psi
xi * phi * chi = zeta * psi
xi * phi = eta * tau
xi * phi = gamma
xi * phi = kappa
xi * phi * psi = eta * chi
xi * phi * psi = eta * rho
xi * phi * psi = eta * theta
xi * phi * psi = mu * nu * sigma
xi * phi * psi = nu * rho * chi
xi * phi * psi = nu * tau * chi
xi * phi * psi = theta * nu * pi
xi * pi * chi = eta * nu * phi
xi * pi * omega = eta * kappa
xi * pi * omega = gamma * phi
xi * pi * omega = gamma * psi
xi * pi * phi = beta * psi
xi * pi * phi = eta * nu
xi * pi * phi = eta * tau
xi * pi * phi = eta * theta
xi * pi * phi = iota * tau
xi * pi * phi = kappa * mu
xi * pi * phi = kappa * nu
xi * pi * phi = mu * sigma
xi * pi * phi = nu * chi * psi
xi * pi * phi = nu * rho
xi * pi * phi = nu * rho * tau
xi * pi * phi = nu * sigma
xi * pi * phi = zeta * psi
xi * pi * psi = alpha * nu
xi * pi * psi = beta * chi
xi * pi * psi = beta * phi
xi * pi * psi = eta * nu
xi * pi * psi = eta * tau
xi * pi * psi = iota * chi
xi * pi * psi = iota * rho
xi * pi * psi = iota * tau
xi * pi * psi = kappa * chi
xi * pi * psi = kappa * mu
xi * pi * psi = kappa * nu
xi * pi * psi = mu * sigma
xi * pi * psi = nu * phi * chi
xi * pi * psi = nu * rho
xi * pi * psi = nu * rho * chi
xi * pi * psi = theta * nu
xi * pi * psi = zeta * chi
xi * pi * psi = zeta * phi
xi * pi * rho = epsilon
xi * pi * rho = kappa * chi
xi * pi * sigma = alpha * chi
xi * pi * sigma = kappa * phi
xi * pi * sigma = mu * tau * phi
xi * pi * sigma = nu * tau * psi
xi * pi * tau = alpha * nu
xi * pi * tau = kappa * nu
xi * psi = eta * tau
xi * psi = gamma
xi * psi = kappa
xi * rho * chi = eta * theta
xi * rho * chi = gamma * phi
xi * rho * chi = iota * phi
xi * rho * chi = iota * tau
xi * rho * chi = kappa * pi
xi * rho * chi = kappa * psi
xi * rho * chi = nu * pi * psi
xi * rho * chi = theta * pi
xi * rho * phi = alpha * pi
xi * rho * phi = beta * eta * pi
xi * rho * phi = eta * theta
xi * rho * phi = gamma * chi
xi * rho * phi = gamma * pi
xi * rho * phi = iota * chi
xi * rho * phi = iota * tau
xi * rho * phi = kappa * psi
xi * rho * phi = kappa * tau
xi * rho * phi = zeta * eta * pi
xi * rho * psi = eta * iota
xi * rho * psi = eta * theta
xi * rho * psi = iota * chi
xi * rho * psi = iota * phi
xi * rho * psi = kappa * chi
xi * rho * psi = kappa * pi
xi * rho * tau = alpha * phi
xi * rho * tau = eta * iota
xi * rho * tau = iota * nu * pi
xi * rho * tau = iota * pi * phi
xi * rho * tau = kappa * pi
xi * rho * tau = theta * nu
xi * sigma = alpha * chi
xi * sigma = beta * eta
xi * sigma = beta * pi
xi * sigma = beta * pi * psi
xi * sigma = beta * psi
xi * sigma = iota * mu
xi * sigma = iota * rho
xi * sigma = kappa * nu
xi * sigma = kappa * psi
xi * sigma = lambda
xi * sigma = nu * pi * phi
xi * sigma = nu * pi * psi
xi * sigma = nu * tau * psi
xi * sigma = theta * pi
xi * sigma = theta * psi
xi * sigma = zeta * eta
xi * sigma = zeta * pi
xi * sigma = zeta * pi * psi
xi * sigma = zeta * psi
xi * tau * chi = alpha * pi
xi * tau * chi = alpha * psi
xi * tau * chi = beta * pi * phi
xi * tau * chi = eta * iota
xi * tau * chi = eta * pi * psi
xi * tau * chi = iota * nu * pi
xi * tau * chi = iota * rho
xi * tau * chi = kappa * nu
xi * tau * chi = kappa * nu * pi
xi * tau * chi = kappa * pi
xi * tau * chi = theta * phi
xi * tau * chi = theta * pi
xi * tau * chi = theta * psi
xi * tau * chi = zeta * pi * phi
xi * tau = iota * nu
xi * tau * phi = eta * iota
xi * tau * phi = eta * iota * pi
xi * tau * phi = eta * pi * rho
xi * tau * phi = iota * rho
xi * tau * phi = theta * chi
xi * tau * psi = alpha * eta
xi * tau * psi = alpha * pi
xi * tau * psi = eta * iota
xi * tau * psi = eta * iota * pi
xi * tau * psi = iota * pi * rho
xi * tau * psi = iota * rho
xi * tau * psi = theta * chi
xi * tau * psi = theta * nu
xi * tau * psi = theta * phi
xi * tau * psi = theta * pi
zeta * chi = beta * nu
zeta * chi = beta * tau
zeta * eta * nu = alpha * beta
zeta * eta * pi = beta * iota
zeta * eta * xi = alpha * beta
zeta * eta * xi = beta * iota
zeta * eta * xi = beta * pi * phi
zeta * eta * xi = beta * pi * psi
zeta * iota = beta * eta * pi
zeta * iota = beta * eta * xi
zeta * iota = beta * nu * pi
zeta * iota = beta * nu * xi
zeta * iota = delta * pi
zeta * iota = delta * rho
zeta * iota = delta * xi
zeta * kappa = beta * theta
zeta * nu = beta * chi
zeta * nu = beta * phi
zeta * nu = beta * psi
zeta * nu = beta * rho
zeta * nu * phi = beta * pi * tau
zeta * nu * pi = beta * iota
zeta * nu * psi = beta * pi * tau
zeta * nu * xi = beta * iota
zeta * phi = alpha * nu
zeta * phi = beta * nu
zeta * phi = beta * tau
zeta * phi = beta * xi * pi
zeta * pi = beta * rho
zeta * pi * phi = alpha * psi
zeta * pi * phi = beta * eta * xi
zeta * pi * phi = gamma * chi
zeta * pi * psi = alpha * phi
zeta * pi * psi = alpha * xi
zeta * pi * psi = beta * eta * xi
zeta * pi * tau = beta * nu * phi
zeta * pi * tau = beta * nu * psi
zeta * pi * tau = beta * theta
zeta * pi * tau = delta * iota
zeta * psi = beta * nu
zeta * psi = beta * tau
zeta * psi = beta * xi * pi
zeta * rho = beta * nu
zeta * rho = beta * pi
zeta * rho = beta * tau
zeta * rho = beta * xi
zeta * tau = beta * chi
zeta * tau = beta * phi
zeta * tau = beta * psi
zeta * tau = beta * rho
zeta * tau = delta * nu
zeta * theta = beta * gamma
zeta * theta = beta * kappa
zeta * theta = beta * pi * tau
zeta * theta = beta * xi * tau
zeta * xi = beta * rho
zeta * xi * phi = alpha * eta
zeta * xi * pi = alpha * eta
zeta * xi * pi = beta * phi
zeta * xi * pi = beta * psi
zeta * xi * tau = beta * theta
zeta * xi * tau = delta * iota
